input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  date {
    match => ["timestamp", "ISO8601"]
  }
}

output {
  elasticsearch {
    hosts => ["http://es:9200"]
    index => "user-activity-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
